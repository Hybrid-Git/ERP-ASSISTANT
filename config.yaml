# ── Pipeline configuration ──────────────────────────────────────
# All hardcoded word lists, thresholds, and constants used in the pipeline are defined here.
# Edit this file to add cities, language hints, or adjust routing.

# --- COMMENTED OUT (zero-regex migration): cities list ---
# cities:
#   - BANGALORE ...

# --- COMMENTED OUT (zero-regex migration): hinglish hints ---
# hinglish:
#   non_english_hints:
#     - batao ...

# --- COMMENTED OUT (zero-regex migration): multilingual words ---
#   multilingual_words:
#     - ka ...

# --- COMMENTED OUT (zero-regex migration): route keywords ---
# route_keywords:
#   - customer ...

# --- COMMENTED OUT (zero-regex migration): connectors ---
# connectors:
#   - aur ...

# --- COMMENTED OUT (zero-regex migration): stop tokens ---
# stop_tokens:
#   - KA ...

# --- COMMENTED OUT (zero-regex migration): segment next keywords ---
# segment_next_keywords:
#   - nykaa ...

thresholds:
  embedding_recall_min: 0.4
  reranker_top_k: 6
  max_records: 10

# --- COMMENTED OUT (zero-regex migration): party_words ---
# party_words:
#   - customer ...

# --- COMMENTED OUT (zero-regex migration): name_words ---
# name_words:
#   - name ...

# --- COMMENTED OUT (zero-regex migration): list_words ---
# list_words:
#   - all ...

pretty_field_names:
  id: ID
  name: Name
  hsnCode: HSN Code
  closingQty: Closing Quantity
  closingRate: Closing Rate
  closingValue: Closing Value
  openingBalance: Opening Balance
  openingType: Opening Type
  voucherCount: Voucher Count
  taxableAmount: Taxable Amount
  invoiceAmount: Invoice Amount
  igst: IGST
  cgst: CGST
  sgst: SGST
  cess: CESS
  tax: Total Tax
  category: Category
  totalQty: Total Quantity
  totalRevenue: Total Revenue
  totalTaxableAmount: Taxable Amount
  totalTaxAmount: Tax Amount
  totalDiscount: Discount
  totalProfit: Profit
  orderCount: Orders
  avgRate: Avg. Rate
  totalAmount: Total Amount
  totalSales: Total Sales
  totalTax: Total Tax
  totalOutstanding: Outstanding
  totalPaid: Paid
  totalIncome: Income
  totalQuantity: Total Quantity
  invoiceCount: Invoices
  averageInvoiceValue: Avg. Invoice Value
  percentage: Growth %
  absolute: Absolute Change
  totalRevenue: Total Revenue
  orderCount: Orders
  totalPurchases: Total Purchases
  billCount: Bills
  totalExpenses: Total Expenses
  averageBillValue: Avg. Bill Value
  glGroup: GL Group
  searchTerm: Search Term
  groupType: Group Type
  invoiceNo: Invoice No.
  invoiceDate: Invoice Date
  dueDate: Due Date
  netAmount: Net Amount
  taxableAmount: Taxable Amount
  outstanding: Outstanding
  paidAmount: Paid
  daysOverdue: Days Overdue
  isOverdue: Overdue
  agingBucket: Aging Bucket
  txModeType: Type
  invoiceType: Invoice Type
  totalInvoices: Total Invoices
  totalOutstanding: Total Outstanding
  totalOverdue: Total Overdue
  totalCurrent: Total Current
  invoiceCategory: Category
  totalReceivables: Total Receivables
  totalPayables: Total Payables
  receivablesCount: Receivable Invoices
  payablesCount: Payable Invoices

# --- COMMENTED OUT (zero-regex migration): identifier patterns ---
# identifier_patterns:
#   invoice_no:
#     - ...

# --- COMMENTED OUT (zero-regex migration): name_extraction ---
# name_extraction:
#   stopwords: [...]

# --- COMMENTED OUT (zero-regex migration): city aliases ---
# city_aliases:
#   bengaluru: BANGALORE ...
